// /dashboard/[slug]/layout.tsx

const NotePageLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <>
      <div className="">{children}</div>
    </>
  );
};

export default NotePageLayout;
